# Rule-Based Platooning Experiment Design

This file defines the paper-facing Chapter 5 experiment design for the formal hS=3 NP/CHP/PP pipeline on branch `agent/restore-rule-based-platooning-clean`.

Do not change the parameter grid, solver settings, figure definitions, or output meanings without updating both this file and `../../paper/sections/experiments.tex`.

The frozen Chapter 5 figure specification is `CHAPTER5_FIGURE_SPEC.md`. Do not change figure axes, plot types, visual encodings, or aggregation rules without explicit project-lead approval.

## Chapter 5 Structure

Chapter 5 contains two studies:

1. Validation of the rule-level upper bound.
2. Validation of the proposed platooning method.

Both studies use the same generated traffic instances and formal grid.

## Shared Traffic Settings

- number of approaches: `L = 4`
- total vehicles: `N in {20, 40, 60, 80}`
- balanced vehicle counts across approaches
- `lambda` means the total vehicle arrival rate into the entire conflict area
- approved total arrival rates: `lambda in {0.5, 1.0, 1.5, 2.0, 2.5}` veh/s
- independent Poisson arrivals on each approach with per-approach rate `lambda / L`
- for `L = 4`, the per-approach rates are `{0.125, 0.250, 0.375, 0.500, 0.625}` veh/s
- minimum following headway: `hF = 1 s`
- minimum switching headway: `hS = 3 s`
- platooning thresholds: `delta in {2, 4, 6, 8}` s
- maximum platoon sizes: `Pmax in {2, 4, 6, 8}`
- replications: `10`
- Gurobi threads: `12`
- common maximum solve time: `600 seconds` for NP, CHP, and PP
- deterministic instance seeds

The same generated traffic instance must be reused by NP, CHP, and every PP parameter combination.

`N = 80` is retained as the largest stress-test scale. It uses the same common solver limit and enters paper-facing aggregates only after all 50 traffic instances are complete.

## Methods

- `NP`: no platooning; every vehicle remains an independent scheduling unit.
- `CHP`: conventional critical-headway platooning using `delta` with no platoon-size cap.
- `PP`: proposed platooning using `delta` and capped by `Pmax`.

The active formal pipeline must not call archived optimized-partition, complete-frontier, sampled-budget, or dimension-budget methods.

## Bound Validation

The paper-facing validation target is:

```text
G(Pi) <= Ghat(delta, Pmax)
```

where:

```text
G(Pi) = D_PP - D_NP
```

The partition-specific bound may remain in diagnostic output but is not the plotted or manuscript-facing validation target.

Bound checks are available only when both NP and PP are proven optimal within the common 600-second limit. Unresolved rows retain their incumbent, best bound, and terminal MIP gap but are excluded from exact-gap checks. There is no separate recovery stage.

Required summary statistics:

- total PP rows
- bound-checkable PP rows
- unique checked instances
- coverage by `(N, total lambda)`
- bound violation count and rate
- mean and median actual gap
- mean and median rule-level upper bound
- mean, median, and minimum bound slack

Manuscript claims about realized gaps must specify that they apply to checked cases when coverage is incomplete.

## Method Validation

The comparison study uses the same 600-second maximum downstream time limit for NP, CHP, and PP. Gurobi stops as soon as optimality is proved. NP is solved once per instance, CHP once per `delta`, and PP once per `(delta, Pmax)`.

Required metrics:

- average vehicle delay or best incumbent objective
- proven-optimal rate
- terminal MIP gap
- downstream solve time
- end-to-end time
- model construction time
- branch-and-bound node count
- number of platoons or scheduling units
- ordering-variable count
- dimension-reduction ratio

The delay ordering

```text
D_NP <= D_PP <= D_CHP
```

is checked only when all three corresponding models are proven optimal.

## Figures

All formal figures are generated by `plot_rule_based_formal_hs3.py`.

- `bound_validation_actual_vs_upper.pdf`: 3D bound validation. The `Ghat` surface is shown over `delta` and `Pmax`; actual `G` points use only bound-checkable rows, and marker shape represents total `lambda`.
- `experimental_tradeoff_solve_time_gap.pdf`: case-level downstream solve time versus actual `G`; `delta` is color and `Pmax` is marker.
- `pp_delay_vs_threshold_density.pdf`: method-level NP/CHP/PP delay means using total `lambda` on the arrival-rate axis; PP is averaged over `Pmax` for this presentation figure.
- `pp_time_and_scheduling_units.pdf`: method-level solve-time and scheduling-unit means using total `lambda` on the arrival-rate panels; solve-time panels use a logarithmic y-axis and scheduling-unit panels use a linear y-axis.
- `gurobi_solution_quality_over_time.pdf`: shared-instance incumbent trajectory for NP, CHP, and representative PP settings.

PDF is the default publication format. PNG previews are optional and generated only with `--write-png`.

## Output Directories

The active formal result directories are:

```text
results/rule_based_experiments/formal_pp_hS3_600s_r10
results/rule_based_experiments/formal_bound_hS3_600s_r10
results/rule_based_experiments/formal_figures_hS3_600s_r10
```

The paper-facing figure PDFs are copied to `paper/figures/`.

Checkpoint directories are local resume artifacts and must not be committed.

## Execution Requirements

Formal scripts must:

- checkpoint after every completed replication
- support resume
- support checkpoint-only parallel chunks
- support appending new checkpoints to existing aggregate CSV files
- save full configuration manifests
- preserve counts and release times
- write deterministic instance identifiers
- detect duplicate aggregate rows
- report Gurobi status, runtime, MIP gap, nodes, and incumbent information
- use the uniform `uniform_600s` run role for every method

Only one process should write aggregate CSV/JSON outputs at a time. Parallel experiment execution must use `--checkpoint-only` chunks followed by a single aggregation pass.

## Current Repository State

The previous aggregate files under `formal_pp_hS3` and `formal_bound_hS3` are legacy 30-second, 20-replication results. For the new uniform design, the first 10 replications of the approved relabeled `lambda={1.5,2.5}` cases may be reused when the legacy solve already proved optimal; unresolved legacy rows are rerun on the same stored instance with the 600-second limit. All other grid points are generated directly under the new `*_600s_r10` result directories. No separate NP-recovery output is produced.

The reuse and relabeling of the `lambda={1.5,2.5}` rows is an approved project decision and is not itself a reason to rerun those cases.
